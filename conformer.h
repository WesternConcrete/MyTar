#ifndef _CONFORMER_H
#define _CONFORMER_H  1

#include <string.h>
#include <arpa/inet.h>
#include <string.h>
#include <arpa/inet.h>

uint32_t extra_special_int(char *where, int len);
int insert_special_character(char *where, size_t size, int32_t val);

#endif
